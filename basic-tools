apt update && apt install -y \
    gdb \
    python3 \
    python3-pip \
    git \
    wget \
    netcat-openbsd \
    binutils \
    checksec

git clone https://github.com/pwndbg/pwndbg
cd pwndbg && ./setup.sh
